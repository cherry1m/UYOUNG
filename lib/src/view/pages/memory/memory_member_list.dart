import 'package:flutter/material.dart';

class MemoryMemberList extends StatelessWidget {
  const MemoryMemberList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
